<div class="wrapper wrapper_padding cleafix">
  <ul>
    <% @categories.each do |category| %>
      <div class="category_content">
        <h2 id="category-header"><%= link_to category.name, category_path(category) %></h2>
      </div>
    <% end %>
  </ul>
</div>

