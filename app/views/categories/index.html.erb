<div class="wrapper wrapper_padding cleafix">
  <ul>
    <% @categories.each do |category| %>
      <div class="category_content">
        <h1 id="category-header"><%= link_to category.name, category_path(category) %></h1>
      </div>
    <% end %>
  </ul>
</div>

